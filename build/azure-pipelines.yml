# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

variables:
  buildConfiguration: 'Release'
  version.patch: $[counter(format('{0}.{1}', variables['MajorVersion'], variables['MinorVersion']), 0)]
  versionNumber: $[format('{0}.{1}.{2}', variables['MajorVersion'], variables['MinorVersion'], variables['version.patch'])]

name: $[format('{0}_{1}', variables['Build.SourceBranchName'], variables['versionNumber'])]

trigger: none

pr: none

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: dotnet restore
  displayName: 'dotnet restore'

- script: dotnet build --configuration $(buildConfiguration) -p:Version=$(versionNumber).0
  displayName: 'dotnet build $(buildConfiguration)'

- task: DotNetCoreCLI@2
  displayName: 'dotnet test $(buildConfiguration)'
  inputs:
    command: test
    projects: '**/*Tests.csproj'
    arguments: '--configuration $(buildConfiguration)'

# Create nuget packages
- script: dotnet pack **/PexCard.Api.Client.Core/PexCard.Api.Client.Core.csproj --configuration $(buildConfiguration) --no-build --output $(Build.ArtifactStagingDirectory) -p:PackageVersion=$(versionNumber)
  displayName: 'dotnet pack PexCard.Api.Client.Core.csproj'

- script: dotnet pack **/PexCard.Api.Client/PexCard.Api.Client.csproj --configuration $(buildConfiguration) --no-build --output $(Build.ArtifactStagingDirectory) -p:PackageVersion=$(versionNumber)
  displayName: 'dotnet pack PexCard.Api.Client.csproj'


# This temporary step is needed to investigate issue with incorrect aseembly version for PexCard.Api.Client.Core.dll
- task: PublishBuildArtifacts@1
  displayName: 'Publish artifacts'
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'


# Push nuget packages
#- script: dotnet nuget push $(Build.ArtifactStagingDirectory)/PexCard.Api.Client.Core.$(versionNumber).nupkg -k $(NugetApiKey) -s https://api.nuget.org/v3/index.json
#  displayName: 'dotnet nuget push PexCard.Api.Client.Core.nupkg'

#- script: dotnet nuget push $(Build.ArtifactStagingDirectory)/PexCard.Api.Client.$(versionNumber).nupkg -k $(NugetApiKey) -s https://api.nuget.org/v3/index.json
#  displayName: 'dotnet nuget push PexCard.Api.Client.nupkg'
